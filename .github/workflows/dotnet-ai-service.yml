name: Ai - Build .NET Service

on:
  push:
    branches: [ main ]    
    paths:
        - 'Hyperdrive.Ai.Service/**'

  pull_request:
    branches: [ main ]
    paths:
        - 'Hyperdrive.Ai.Service/**'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: Hyperdrive.Ai.Service

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v5

      - name: 🛠️ Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: 📥 Restore dependencies
        run: dotnet restore --locked-mode Hyperdrive.Ai.Service/Hyperdrive.Ai.Service.csproj

      - name: ♻️ Clean project
        run: dotnet clean Hyperdrive.Ai.Service/Hyperdrive.Ai.Service.csproj

      - name: 🏗️ Build project
        run: dotnet build --no-restore Hyperdrive.Ai.Service/Hyperdrive.Ai.Service.csproj    
